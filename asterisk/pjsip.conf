; pjsip.conf
; Static shell - dynamic content generated by run.sh and included below.
; run.sh writes credentials/transport config to /tmp/voip/pjsip_dynamic.conf
; and then reloads PJSIP so it picks up the new config.

#include /tmp/voip/pjsip_dynamic.conf
